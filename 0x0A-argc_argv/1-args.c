#include<stdio.h>
#include "main.h"
/**
 * main - function
 * @argc: number of lines
 * @argv: array to pointer
 * Return: always 0
 */
int main(int argc, char *argv[] __attribute__((unused)))
{
printf("%d \n", argc - 1);
return (0);
}
