#include "main.h"
#include<stdio.h>

/**
 * _isdigit - check digit
 * @c: the number 
 * Return 1 if true 0 if fals
 */

int _isdigit(int c)
{
	if (c <= 9 && c >= 1)
	{
		return (1);
	}
	else 
	{
		return (0);
	}
	return (0);
}
