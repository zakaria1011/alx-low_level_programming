#include "main.h"
#include <stdio.h>

/**
 * mul - multiplication of two numbers
 *@a: first number.
 *@b: second number
 * Return: value of multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
